export default function HomePage() {
  return (
    <main style={{ maxWidth: 680, margin: "4rem auto", padding: "0 1.25rem" }}>
      <h1>Ask Your Hiring Data</h1>
      <p style={{ color: "var(--muted)" }}>
        Project scaffold. The chat UI, chart rendering, and role switcher land in a later build
        step. The query-IR validation boundary, the deterministic executor with server-side role
        scoping, and the CI-gated eval suite come first — in that order.
      </p>
    </main>
  );
}
